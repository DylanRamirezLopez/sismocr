# Render Blueprint — auto-deploy API + Worker + PostgreSQL + Redis
# Conecta tu repo en https://dashboard.render.com/select-repo
databases:
  - name: sismocr-db
    databaseName: sismocr
    user: sismocr
    plan: free
    region: oregon
    ipAllowList: []

services:
  - type: redis
    name: sismocr-redis
    plan: free
    region: oregon
    maxmemoryPolicy: allkeys-lru
    ipAllowList: []

  - type: web
    name: sismocr-api
    runtime: docker
    region: oregon
    plan: free
    branch: main
    rootDir: backend
    dockerContext: .
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sismocr-db
          property: connectionString
      - key: REDIS_URL
        fromRedis:
          name: sismocr-redis
          property: connectionString
      - key: DEBUG
        value: "false"
      - key: LOG_LEVEL
        value: INFO
      - key: ENABLE_API_V2
        value: "true"

  - type: worker
    name: sismocr-worker
    runtime: docker
    region: oregon
    plan: free
    branch: main
    rootDir: backend
    dockerContext: .
    startCommand: python -m app.workers.ingestion_worker
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: sismocr-db
          property: connectionString
      - key: REDIS_URL
        fromRedis:
          name: sismocr-redis
          property: connectionString
      - key: LOG_LEVEL
        value: INFO
